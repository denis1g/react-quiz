import React from 'react';
export default ({name, side}) => (
	<li>{name} - <strong>{side}</strong></li>
)

// const Character = (props) => {
// 	return (
// 		<li>{props.name} + {props.side}</li>
// 	)
// };

